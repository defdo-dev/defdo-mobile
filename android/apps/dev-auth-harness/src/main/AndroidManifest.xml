<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="Defdo Auth Dev Harness"
        android:supportsRtl="true"
        android:theme="@style/Theme.DefdoAuthHarness">

        <activity
            android:name=".DevAuthHarnessActivity"
            android:exported="true"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="${defdoAuthRedirectScheme}"
                    android:host="${defdoAuthRedirectHost}"
                    android:pathPrefix="${defdoAuthRedirectPathPrefix}" />
            </intent-filter>
        </activity>
    </application>
</manifest>
